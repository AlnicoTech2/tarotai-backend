version: 1.0
runtime: python313
build:
  commands:
    build:
      - pip install uv
      - uv sync --no-dev
run:
  command: uv run uvicorn main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
  env:
    - name: APP_ENV
      value: production
